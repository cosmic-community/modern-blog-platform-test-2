/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'cdn.cosmicjs.com',
      },
      {
        protocol: 'https',
        hostname: 'imgix.cosmicjs.com',
      },
    ],
  },
  // Never enable typedRoutes - causes complex TypeScript errors
  experimental: {
    typedRoutes: false,
  },
}

module.exports = nextConfig